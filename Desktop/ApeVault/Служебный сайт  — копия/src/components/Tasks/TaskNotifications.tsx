// Placeholder - file removed but kept for git compatibility
interface TaskNotificationsProps {
  onTaskClick?: (taskId: string) => void
}

export const TaskNotifications = (_props: TaskNotificationsProps) => null

