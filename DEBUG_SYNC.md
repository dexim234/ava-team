# Отладка синхронизации Call между сайтами

## Проблема: Сигналы создаются на служебном сайте, но не отображаются на students-app

### Шаг 1: Проверьте консоль браузера

Откройте DevTools (F12) на обоих сайтах и проверьте логи:

**На служебном сайте** (при создании сигнала):
- Должны быть логи: `📝 Service site: Creating call:`
- Должен быть лог: `✅ Service site: Call created successfully with ID:`

**На students-app** (на странице Club):
- Должен быть лог: `🔥 Students-app Firebase config:`
- Должен быть лог: `✅ Students-app Firestore initialized: true`
- Должен быть лог: `🔔 Subscribing to calls with filters:`
- Должен быть лог: `📥 Received X documents from Firestore`

### Шаг 2: Проверьте Firebase проект

Убедитесь, что оба сайта используют **один и тот же Firebase проект**:

1. Откройте консоль браузера на **служебном сайте**
2. Найдите лог: `Firebase config check:` и запомните `projectId`
3. Откройте консоль браузера на **students-app**
4. Найдите лог: `🔥 Students-app Firebase config:` и проверьте `projectId`
5. **Они должны совпадать!**

Если не совпадают:
- Проверьте файлы `.env` в обоих проектах
- Убедитесь, что `VITE_FIREBASE_PROJECT_ID` одинаковый

### Шаг 3: Проверьте Firebase Console

1. Зайдите в [Firebase Console](https://console.firebase.google.com)
2. Выберите ваш проект
3. Перейдите в **Firestore Database**
4. Откройте коллекцию `calls`
5. Убедитесь, что там есть документы с созданными сигналами

### Шаг 4: Проверьте правила Firestore

В Firebase Console → Firestore Database → Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /calls/{callId} {
      allow read: if true; // ВАЖНО: должно быть true для чтения
      allow create, update, delete: if request.auth != null;
    }
  }
}
```

### Шаг 5: Временно отключите фильтр "Только активные"

На students-app:
1. Зайдите на страницу `/club`
2. Снимите галочку **"Только активные (<24ч)"**
3. Проверьте, появились ли сигналы

Если сигналы появились после отключения фильтра:
- Проблема в фильтре по дате (24 часа)
- Сигналы созданы более 24 часов назад
- Решение: создайте новый тестовый сигнал или отключите фильтр

### Шаг 6: Проверьте структуру данных

В Firebase Console проверьте структуру документа в коллекции `calls`:

Должны быть поля:
- `userId` (string)
- `network` (string)
- `ticker` (string)
- `pair` (string)
- `entryPoint` (string)
- `target` (string)
- `strategy` (string)
- `risks` (string)
- `createdAt` (string, ISO format)
- `status` (string, должно быть "active")

### Шаг 7: Проверьте индексы Firestore

Если в консоли есть ошибка про индексы:
1. Firebase Console → Firestore Database → Indexes
2. Создайте составной индекс для:
   - Коллекция: `calls`
   - Поля: `status` (Ascending), `createdAt` (Descending)

### Частые проблемы и решения

#### Проблема: "Permission denied"
**Решение:** Проверьте правила Firestore (см. Шаг 4)

#### Проблема: "Index required"
**Решение:** Создайте индекс в Firebase Console (см. Шаг 7)

#### Проблема: Разные projectId
**Решение:** Убедитесь, что `.env` файлы содержат одинаковые значения

#### Проблема: Сигналы не обновляются в реальном времени
**Решение:** 
- Проверьте подключение к интернету
- Проверьте, что используется `onSnapshot` (реализовано)
- Перезагрузите страницу students-app

#### Проблема: Фильтр скрывает все сигналы
**Решение:**
- Отключите фильтр "Только активные (<24ч)"
- Создайте новый тестовый сигнал
- Проверьте, что `status` = "active" и `createdAt` не старше 24 часов

### Тестирование

1. **Создайте тестовый сигнал** на служебном сайте:
   - Зайдите на `/call`
   - Нажмите "Создать новый сигнал"
   - Заполните все обязательные поля
   - Сохраните

2. **Проверьте в Firebase Console:**
   - Документ должен появиться в коллекции `calls`
   - Проверьте все поля

3. **Проверьте на students-app:**
   - Зайдите на `/club`
   - Откройте вкладку "Лента"
   - Отключите фильтр "Только активные" (для теста)
   - Сигнал должен появиться автоматически

4. **Проверьте консоль браузера:**
   - Должны быть логи о подписке
   - Должны быть логи о получении данных
   - Не должно быть ошибок

### Дополнительная информация

Если проблема не решена, соберите следующую информацию:

1. Скриншоты консоли браузера на обоих сайтах
2. Скриншот Firebase Console с коллекцией `calls`
3. Скриншот правил Firestore
4. Значения `projectId` из логов обоих сайтов


